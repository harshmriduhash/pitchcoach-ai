import { createFileRoute } from "@tanstack/react-router"; import { useEffect,useState } from "react"; import { Medal } from "lucide-react"; import { AppShell } from "@/components/app-shell"; import { supabase } from "@/integrations/supabase/client";
type Row={user_id:string;leaderboard_score:number;total_calls:number;avg_score_overall:number;profiles:{name:string;role:string}|null};
export const Route=createFileRoute("/leaderboard")({component:Leaderboard,head:()=>({meta:[{title:"Sales Practice Leaderboard | PitchCoach AI"},{name:"description",content:"See the sales reps building the strongest practice habits."}]})});function Leaderboard(){const [rows,setRows]=useState<Row[]>([]);useEffect(()=>{void supabase.from("user_stats").select("user_id,leaderboard_score,total_calls,avg_score_overall,profiles(name,role)").order("leaderboard_score",{ascending:false}).limit(50).then(({data})=>setRows((data as Row[]|null)??[]))},[]);return <AppShell><p className="text-sm font-bold uppercase tracking-[.2em] text-primary">Sales hall of fame</p><h1 className="mt-2 text-6xl font-black">OUTWORK THE FIELD.</h1><p className="mt-3 text-muted-foreground">Rankings reward score, improvement, and consistency—not raw volume.</p><div className="mt-10 overflow-hidden rounded-xl border border-border bg-card"><div className="grid grid-cols-[70px_1fr_90px_90px] border-b border-border px-5 py-3 text-xs font-bold uppercase text-muted-foreground"><span>Rank</span><span>Rep</span><span>Calls</span><span>Score</span></div>{rows.length?rows.map((r,i)=><div key={r.user_id} className="grid grid-cols-[70px_1fr_90px_90px] items-center border-b border-border px-5 py-4 last:border-0"><span className="font-display text-2xl text-primary">{i<3?<Medal className="size-5"/>:`#${i+1}`}</span><span><b>{r.profiles?.name||"Anonymous Rep"}</b><small className="ml-2 uppercase text-muted-foreground">{r.profiles?.role}</small></span><span>{r.total_calls}</span><b className="font-display text-2xl">{Math.round(r.leaderboard_score)}</b></div>):<div className="p-14 text-center text-muted-foreground">The board is open. Complete a call to claim the first spot.</div>}</div></AppShell>}