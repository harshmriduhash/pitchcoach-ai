import { createFileRoute } from "@tanstack/react-router";import { AppShell } from "@/components/app-shell";export const Route=createFileRoute("/analytics")({component:Analytics,head:()=>({meta:[{title:"Analytics | PitchCoach AI"}]})});function Analytics(){return <AppShell><p className="text-sm font-bold uppercase tracking-[.2em] text-primary">Performance lab</p><h1 className="mt-2 text-6xl font-black">YOUR EDGE, MEASURED.</h1><div className="mt-10 rounded-xl border border-border bg-card p-12 text-center"><p className="font-display text-5xl text-primary">0 → 100</p><p className="mx-auto mt-3 max-w-lg text-muted-foreground">Complete practice calls to unlock trends across opening, discovery, talk ratio, objection handling, value framing, next step, filler words, and confidence.</p></div></AppShell>}